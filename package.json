{
  "name": "wp-starter-woocommerce",
  "version": "1.0.0",
  "description": "WP Starter WooCommerce Theme",
  "scripts": {
    "sass": "sass --watch assets/scss:assets/css --style compressed",
    "build": "sass assets/scss:assets/css --style compressed"
  },
  "devDependencies": {
    "sass": "^1.69.5"
  }
}
